package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Pulse Fitness Professional Polish Material Palette
val PolishBackground = Color(0xFFFBFCFF)   // Warm clean white-blue background
val PolishOnBackground = Color(0xFF191C1E) // Dark slate for primary typography
val PolishTextSecondary = Color(0xFF40484C)// Medium slate for subheadings/metadata

// Brand Accents
val PolishPrimary = Color(0xFF006A6A)     // Premium Teal for main actions, coaches, headers
val PolishSecondary = Color(0xFF004977)   // Deep Ocean Blue
val PolishTertiary = Color(0xFF34C759)    // Fit Green accent

// Highlight containers (from the HTML header & cards)
val PolishBlueContainer = Color(0xFFD1E4FF)    // Soft Active blue container
val PolishOnBlueContainer = Color(0xFF001D36)  // Extremely dark blue contrast

val PolishTealContainer = Color(0xFF006A6A)    // Deep Coach Container
val PolishOnTealContainer = Color(0xFFBCEBEB)  // Sharp mint contrast label
val PolishTeelButtonText = Color(0xFF002020)   // Mint-button dark text

// Surface / Structural Colors
val PolishSurface = Color(0xFFFFFFFF)          // Crisp clean white cards
val PolishSurfaceVariant = Color(0xFFF2F0F4)   // Soft warm-gray secondary cards
val PolishBorder = Color(0xFFC1C7CE)           // Subtle border line for structured inputs

// Dark Mode Fallbacks or equivalents adjusted for consistency
val DarkBackground = Color(0xFF0B0D13)
val DarkSurface = Color(0xFF171A26)
val DarkSurfaceVariant = Color(0xFF232738)
val DarkOnBackground = Color(0xFFF2F2F7)
val DarkOnSurface = Color(0xFFE5E5EA)

